using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CommunicateScript : MonoBehaviour
{

    Test other;
    // Start is called before the first frame update
    void Start()
    {
        other.TestFuction();
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
